#spring.cloud.config.name=neo-config
#spring.cloud.config.profile=dev
#spring.cloud.config.label=master
spring.cloud.config:
        name: client
        profile: dev 
        label: master
        discovery:
                  enabled: true
                  serviceId: eureka-provider&config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
#test: test

